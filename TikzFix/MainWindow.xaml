<Window x:Class="TikzFix.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:TikzFix"
        xmlns:views="clr-namespace:TikzFix.Views"
        xmlns:vm="clr-namespace:TikzFix.VM"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>

    <Window.Resources>
        <views:MouseEventToCanvasEventConverter x:Key="MouseEventConverter"></views:MouseEventToCanvasEventConverter>
    </Window.Resources>
    <Grid>

        <views:CollectionCanvas x:Name="MainCanvas" Shapes="{Binding Shapes, Mode=OneWay}" Background="Transparent" CanvasSelectable="{Binding CanvasSelectable}" SelectedShapes="{Binding SelectedShapes}">
            <b:Interaction.Triggers>

                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding StepDrawingCommand}" EventArgsConverter="{StaticResource MouseEventConverter}"></b:InvokeCommandAction>
                </b:EventTrigger>

                <b:EventTrigger EventName="MouseLeftButtonUp">
                    <b:InvokeCommandAction Command="{Binding StepDrawingCommand}" EventArgsConverter="{StaticResource MouseEventConverter}"></b:InvokeCommandAction>
                </b:EventTrigger>

                <b:EventTrigger EventName="MouseMove">
                    <b:InvokeCommandAction Command="{Binding UpdateDrawingCommand}" EventArgsConverter="{StaticResource MouseEventConverter}"></b:InvokeCommandAction>
                </b:EventTrigger>

                <b:KeyTrigger Key="Delete">
                    <b:InvokeCommandAction Command="{Binding DeleteSelectionCommand}"></b:InvokeCommandAction>
                </b:KeyTrigger>

                <b:KeyTrigger Key="Esc">
                    <b:InvokeCommandAction Command="{Binding CancelSelectionCommand}"></b:InvokeCommandAction>
                </b:KeyTrigger>

            </b:Interaction.Triggers>
        </views:CollectionCanvas>

    </Grid>
</Window>
